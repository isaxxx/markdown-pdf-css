{
  "name": "markdown-pdf-css",
  "version": "1.0.0",
  "description": "個人的に利用しているMarkdownをPDFに変換するときのCSS。",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "git-ignore": "git rm -r --cached . && git add -A && git commit --allow-empty -m 'gitignore-refresh' && git reset --soft HEAD^",
    "git-commit": "git add -A && git commit",
    "git-push": "git push origin master"
  },
  "author": "isaxxx",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1",
    "ejs": "^2.6.1",
    "fs-extra": "^6.0.1",
    "markdown-pdf": "^9.0.0",
    "rimraf": "^2.6.2"
  }
}
